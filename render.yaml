services:
  - type: web
    name: telegram-bot
    env: python
    buildCommand: "pip install -r requirements.txt && python -m pip install --upgrade pip"
    startCommand: "uvicorn bot:fastapi_app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PORT
        value: 10000  # أو أي منفذ تفضله
